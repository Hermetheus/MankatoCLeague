import React from "react";
import Photos from "../components/Photos";

const PhotoPage = ({ theme }) => {
  return (
    <div>
      <Photos theme={theme} />{" "}
    </div>
  );
};

export default PhotoPage;
